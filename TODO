yield from
['assert', 'async', 'await', 'class', 'raise']
битовые операции
match/case
фикс чисел в math в конспекте
Python имеет и встроенную команду round(). функция а не команда
функция генераторов и итераторов